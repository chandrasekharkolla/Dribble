<div class="col-md-4">
	<% @posts.each do |post| %>
		<%= link_to image_tag(post.image.url(:small), class: 'img-circle'), post_path(post) %>
		<h1><%= link_to post.title, post_path(post) %></h1>
	<% end %> 
</div>